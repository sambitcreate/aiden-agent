/** Pinned cua-driver 0.8.3 cannot bind writes to a document lifetime. */
export const FORM_FILL_MUTATION_AVAILABLE = false;
export const FORM_FILL_UNAVAILABLE_MESSAGE =
  "Form fill is unavailable because Computer Use cannot yet verify that the approved document is still open. No fields were changed.";
