# Telegram HTML chunk integrity — 2026-09-19

- Baseline `5cc831a17aa8971fb5b89c7dd9278a6ec4022beb`: five new regression groups fail because `hardSplit` slices serialized HTML and `balancePreTags` only repairs pre counts. Inline tags, entities, surrogate pairs, nested pre/code and code whitespace can be damaged.
- `chunkForTelegram` now scans the converter's restricted HTML tokens, counts rendered UTF-16 units with the existing 64-unit headroom, closes/reopens active tags including attributes, and prefers paragraph/line boundaries without deleting whitespace. State consists of active linked tags and at most one rendered-text budget of source boundaries; no DOM, recursive parser, or network.
- HTML source length can exceed 4096 because Telegram's limit applies after entity parsing. Numeric entities count one or two UTF-16 units; astral code points remain indivisible. Rich Markdown chunking and final-edit recovery are unchanged.
- Seven regression groups in the existing registered `telegram-markdown.test.ts` verify exact rendered text and per-character formatting, long converter output, code language/indentation, giant links, exact boundaries, entities/Unicode, and 12 deterministic mixed-format fuzz messages. Telegram suite: 194/194 pass; type-check, scoped lint and diff checks pass.
- Conceptual reference only: Hermes `69ae247cf3dba34a37ab4af8484b96d3559a4fcf`, `gateway/platforms/base.py:7098` and `plugins/platforms/telegram/adapter.py:4572`: carry formatting across chunks and apply platform-specific UTF-16 measurement. No copied implementation.
- Official contract checked at https://core.telegram.org/bots/api#sendmessage and https://core.telegram.org/bots/api#html-style on 2026-09-19. Synthetic tests only; live Telegram acceptance remains untested.
- Lane 22 confirms disjoint ownership: PR157 changes final-edit recovery in `telegram-service-core.ts`. This lane changes only HTML chunking and its tests; no shared server/native contract, UI or setup change. Existing plan status is unchanged.

- Independent Luna review found combining-mark and ZWJ splits at `61742a44`; the new grapheme regression fails that head. A bounded `Intl.Segmenter` pass over the current text budget plus one code point of lookahead now moves hard cuts to grapheme boundaries, including numeric entities and tag seams. Flags, skin tones, keycaps and family emoji are covered. A single grapheme larger than the budget splits only at code-point boundaries to preserve progress and the hard limit.


## Final pre-parse byte-limit correction

- Primary Telegram Bot API server source `tdlib/telegram-bot-api@e3e9dd8e5b3d7ab8537cd5a10dc31d5ffa8f82d1`, `Client::get_formatted_text`, rejects `text.size() > (1 << 15)` before HTML parsing. The prior review considered only the documented 4096 parsed-character budget and missed this second limit.
- Exact prior head `a4c9447f`: `markdownToTelegramHtml("> x\n".repeat(1250))` has 33,749 UTF-8 bytes but only 2,499 rendered UTF-16 units. `deliverHtmlReply` sends the chunker output directly with HTML parse mode, so the server rejects the old single chunk.
- Chunk selection now also reserves serialized UTF-8 bytes for actual tokens, carried opening tags, and all closing tags. Byte-boundary grapheme lookahead crosses formatting tags; normal text, entities, links, code whitespace, and formatting remain intact.
- If an indivisible wrapper/entity cannot fit with even one text token, the message is represented as plain escaped text and rechunked. Visible text is retained; link labels retain destinations as `label (destination)` unless the label already equals the destination. This exceptional fallback intentionally sacrifices formatting to avoid silently losing the response or the destination. Oversized numeric entities normalize before retry, guaranteeing progress.
- Existing registered test file now has 27 cases; five added regressions fail exact prior head (22 pass / 5 fail), all 27 pass with correction. Full Telegram suite: 200/200; application type-check, scoped ESLint, and diff check pass. No live Telegram request or credentials used. No native/client/onboarding/shared-server contract changes.
- Closeout CI wiring audit found the existing `npm test` script used `npm run test:custom-model-options test:telegram`, which passed `test:telegram` as an argument and skipped that suite. Replaced that duplicate custom-options invocation with `npm run test:telegram`; the earlier custom-options command remains. Hosted CI now executes the registered Telegram regressions.
