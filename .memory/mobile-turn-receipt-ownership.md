# Android turn receipt ownership follow-up

Separate worktree /Users/sambitbiswas/.codex/worktrees/mobile-turn-receipt-ownership/aiden-agent, branch feature/mobile-turn-receipt-ownership, based on green PR225 head152577eeb0e4e7350d2f57cdf69d125cb5aa6d22. Coordinator explicitly requested keeping225 stable and publishing the next cohesive slice separately. Original217/225 worktrees untouched.

Executed baseline: actual sender POST is held while another production detail owner accepts a settled transcript. Releasing the successful receipt on225 overwrites the settled assistant with only the accepted user message. /tmp/aiden-turn-receipt-baseline.log and /tmp/aiden-turn-receipt-baseline.patch capture the failure. This is an executed current-parent negative, not a new original-main execution.

Current implementation reserves before POST, atomically admits the receipt under independent removal/purge authority, merges the accepted message by ID and canonical timestamp without replacing winner fields, and maintains separate per-chat receipt/stream ownership so an older receipt cannot replace a newer marker. Fresh detail merge authority retains the original summary ticket to avoid stale title/list projection. In-memory stream ownership survives disk-write failure; accepted202 does not restore a retryable draft merely because persistence fails. Retained original installation/device may retain the receipt for later navigation, while consumer publication requires the original active client identity. No mutation replay.

Actual two-owner regression covers a newer transcript with/without the accepted receipt, newer second POST stream ownership, purge, remove plus fresh readmission, chat disk failure and stream disk failure.185Android JVM tests/lint/instrumentation compilation pass (/tmp/aiden-turn-receipt-android.log). Both independent Sol medium reviews in progress. No iOS/server contract changes. iOS rename/list/Bot response ownership and physical acceptance remain assigned; device locked, no test launch.

Review follow-up: unpair and active-client changes reconcile the optimistic chat and clear queued state; same-ID pairing replacement is fenced using captured creation time before cache admission. Added held unpair/switch/re-pair modes. Seeded an older durable stream marker, failed its replacement before IO, and verified the memory winner survives while reopened disk cannot resume the old stream. Deleting the old marker is best effort if storage itself cannot accept deletion. Full185 JVM tests (0failures/errors/skips), lint and instrumentation compilation pass in /tmp/aiden-turn-review-android.log; focused regression /tmp/aiden-turn-review-fixed.log. Both independent GPT-5.6 Sol medium re-reviews clear the actual final source/tests. No physical execution claimed.

## Inverse completion order correction

Integration Sol identified and owner reproduced on published85f278fe: POST reserves its request ticket, a later GET reserves another ticket and holds a settled response, POST receipt completes first and mints a fresh detail write ticket, then GET is rejected and loses settled assistant/title. Actual two-ViewModel held-HTTP failure: /tmp/aiden-turn-inverse-baseline.log; exact regression patch /tmp/aiden-turn-inverse-baseline.patch. This is an executed227-parent negative.

Receipt admission now merges under original request authority and preserves max(original request, prior admitted snapshot) high-water. Persistence is a private monitor-held operation, not a new response-time request. Stream ownership remains its separate original POST receipt high-water. Receipt activity projection likewise retains existing/request summary authority, so it cannot invalidate a later GET title. Both completion orders are tested, including reopened detail/title, accepted stream marker and one POST/no retryable draft. Existing deletion, newer POST, client replacement and disk failure modes remain covered. Validation/re-review pending for this correction.

Second independent review added inverse_missing: the already-reserved GET may lack the accepted user receipt, even when its other fields are authoritative. Executed negative /tmp/aiden-turn-inverse-missing-baseline.log, focused fixed /tmp/aiden-turn-inverse-missing-fixed.log. Cache retains a receipt overlay only through the latest request ticket observed when its202 is accepted; this observes the clock without reserving authority. Already-in-flight admitted snapshots merge missing receipt IDs while preserving their own title/revision/other messages. The first snapshot requested after acceptance retires the overlay and establishes a high-water that also rejects older held reads. Remove/purge clear overlays. Added fresh retirement/remove/purge and late-old-read controls. Final186 JVM tests, lintDebug and instrumentation compilation pass /tmp/aiden-turn-inverse-final-android.log. Final independent re-reviews completed after the multi-receipt correction below.

Final multi-receipt review found POST1-first/POST2-second could use POST2's newer but pre-receipt candidate and drop receipt1. Actual two-VM held-both-POST negative: /tmp/aiden-turn-ordered-posts-baseline.log. Receipt admission now folds all pending accepted messages, independently of GET cutoff retirement. Added both POST completion orders and asserts exactly two POSTs, both message IDs once, and stream2 ownership. Final checks /tmp/aiden-turn-ordered-final-android.log; no mutation replay or server/iOS contract change.

Final acceptance for this correction:186 JVM tests,0 failures/errors/skips, lintDebug and instrumentation compilation pass. Both independent GPT-5.6 Sol medium final reviews clear after inspecting both POST orders and both GET orders. Hosted CI required on the new commit; physical acceptance remains unexecuted.
