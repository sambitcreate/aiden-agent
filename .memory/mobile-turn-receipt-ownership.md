# Android turn receipt ownership follow-up

Separate worktree /Users/sambitbiswas/.codex/worktrees/mobile-turn-receipt-ownership/aiden-agent, branch feature/mobile-turn-receipt-ownership, based on green PR225 head152577eeb0e4e7350d2f57cdf69d125cb5aa6d22. Coordinator explicitly requested keeping225 stable and publishing the next cohesive slice separately. Original217/225 worktrees untouched.

Executed baseline: actual sender POST is held while another production detail owner accepts a settled transcript. Releasing the successful receipt on225 overwrites the settled assistant with only the accepted user message. /tmp/aiden-turn-receipt-baseline.log and /tmp/aiden-turn-receipt-baseline.patch capture the failure. This is an executed current-parent negative, not a new original-main execution.

Current implementation reserves before POST, atomically admits the receipt under independent removal/purge authority, merges the accepted message by ID and canonical timestamp without replacing winner fields, and maintains separate per-chat receipt/stream ownership so an older receipt cannot replace a newer marker. Fresh detail merge authority retains the original summary ticket to avoid stale title/list projection. In-memory stream ownership survives disk-write failure; accepted202 does not restore a retryable draft merely because persistence fails. Retained original installation/device may retain the receipt for later navigation, while consumer publication requires the original active client identity. No mutation replay.

Actual two-owner regression covers a newer transcript with/without the accepted receipt, newer second POST stream ownership, purge, remove plus fresh readmission, chat disk failure and stream disk failure.185Android JVM tests/lint/instrumentation compilation pass (/tmp/aiden-turn-receipt-android.log). Both independent Sol medium reviews in progress. No iOS/server contract changes. iOS rename/list/Bot response ownership and physical acceptance remain assigned; device locked, no test launch.

Review follow-up: unpair and active-client changes reconcile the optimistic chat and clear queued state; same-ID pairing replacement is fenced using captured creation time before cache admission. Added held unpair/switch/re-pair modes. Seeded an older durable stream marker, failed its replacement before IO, and verified the memory winner survives while reopened disk cannot resume the old stream. Deleting the old marker is best effort if storage itself cannot accept deletion. Full185 JVM tests (0failures/errors/skips), lint and instrumentation compilation pass in /tmp/aiden-turn-review-android.log; focused regression /tmp/aiden-turn-review-fixed.log. Both independent GPT-5.6 Sol medium re-reviews clear the actual final source/tests. No physical execution claimed.
