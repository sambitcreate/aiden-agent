# Project History

### 2026-07-18 — Rename the app to Aiden Agent and establish its GitHub repository
- **Goal:** Replace the original app branding everywhere and publish the project as `sambitcreate/aiden-agent` without carrying over the old local Git history.
- **What was done:** Renamed product/package metadata, UI copy, route title, MCP client and OAuth identity, layout storage keys, localStorage keys, README, AGENTS, and project memory to Aiden Agent / `aiden-agent`. Updated portable-export ignores to cover any `.glaze` bundle.
- **Repository decision:** Start a fresh `main` history for the empty private GitHub repository and preserve the previous `.git` directory only as a recoverable local Trash backup.

### 2026-07-18 — Make the Glaze CLI wrapper work from the standalone repository
- **Goal:** Fix `npm run build` after moving the source out of Glaze's managed Application Support directory.
- **Root cause:** `glaze.ts` only searched relative paths that are valid inside the framework monorepo or Glaze-managed app layout, so the standalone repository resolved them to `/Users/sambitbiswas/projects/glaze-core` and `/Users/sdk`.
- **What was done:** Added portable fallbacks for Glaze's SDK cache under the current user's `Library/Application Support` and the SDK bundled inside `/Applications/Glaze.app`. The wrapper provisions an ignored `node_modules/@glaze/core` symlink so child-process TypeScript and ESLint resolution works outside Glaze's managed layout. Added the SDK global declarations to `tsconfig.json`, a local ESLint config that excludes the private portable export, and updated the README development note.
- **Dev follow-up:** The SDK component bundle imports `sonner`, but Glaze's scaffold did not include it in this app's dependencies. Added `sonner@^2.0.7` and ignored the generated `.devserverhost` marker so standalone Vite dependency scanning succeeds.
- **Native-host limitation:** Running `npm run dev` directly in Terminal starts the backend and Vite successfully, but Glaze 0.10 does not attach its native host in that launch mode. Native bridge calls (`theme.getInfo`, `screen.getAllDisplays`, menus) therefore time out. Full native development must be launched through Glaze; standalone Terminal build/type-check/lint are supported.

### 2026-07-18 — Repository documentation and local Git handoff
- **Goal:** Preserve the Glaze-created Aiden Agent source as a normal local Git repository and document the app, architecture, stack, setup, and security boundaries.
- **What was done:** Added root `AGENTS.md` and `README.md`; made `.memory/` the tracked source of project context; documented Glaze 0.10, React/TypeScript, Pi, MCP, workspace tools, model providers, and Parakeet voice; ignored `.papercuts/` and the portable `Aiden Agent.glaze/` export.
- **Key decision:** The portable export stays outside Git because it includes an older nested Git checkout, generated binaries, chat transcripts, logs, screenshots, and encrypted application-support state. The source tree at the repository root is the version-controlled project.

### 2026-07-18 — Soften dark-mode background from pure black to graphite
- **Goal:** User disliked the pure-black dark theme (opaque black composer bar over the graphite window material); wanted a softer neutral look like a reference screenshot.
- **What was done:** Override dark seeds `--bg`/`--bg-secondary` from #000 → graphite (`#2a2a2c` / `#1d1d1f`) so all bg/text/border ramps recompute and opaque surfaces (composer footer via `bg-background`=`var(--bg)`) blend with the window.
- **Key decisions:** Injected via a runtime `<style>` (`renderer/lib/dark-theme-overrides.ts`, `applyDarkThemeOverrides()`, imported in `main/index.tsx` + `settings/index.tsx`) targeting `html.dark`.
- **Corrections/Lessons Learned:** **Tailwind v4's compiler strips/consolidates redefinitions of its managed seed vars (`--bg`, `--bg-secondary`) written in styles.css** — a `.dark{--bg:…}` (and even higher-specificity `html.dark`) override compiled to *nothing* (identical output hash), while adjacent non-seed rules (hljs) survived. Runtime `<style>` injection bypasses the compiler and wins the cascade. Verified live: `--bg`=#2a2a2c, composer bg = rgb(42,42,44).
- **UI elements:** window background, composer footer.

### 2026-07-17 — Git branch picker, .agents skills, MCP reload + hosted OAuth

- **Goal:** From a reference screenshot + asks: composer shows folder·Local·branch (git only when a repo) with an interactive branch picker; auto-discover Agent Skills from `.agents` folders; a Reload control so MCP/tool changes apply; and full browser OAuth for hosted MCPs (Composio).
- **What was done:** (1) `git.ts` gained uncommitted count + `gitBranches`/`gitCheckout`/`gitCreateBranch`; `git:*` IPC in workspaces.ts; new `GitBranchPicker` (search + checkout + create-and-checkout) wired into composer; added static `Local` badge (`Monitor`). (2) `skills-discovery.ts` scans workspace + global `~/.agents/*/SKILL.md`, merged into `buildAgentTools`; surfaced read-only in Skills settings with source badges. (3) `mcp:reconnect` (closeAll) + Reload button in MCP settings. (4) `mcp-oauth.ts`/`mcp-oauth-store.ts` = MCP SDK OAuth (PKCE + dynamic registration) via loopback redirect (fixed port 41390), `shell.openExternal`, encrypted token store; `oauth` flag on `McpServer`; Authorize button + OAuth toggle in the MCP editor; non-interactive provider on background connects.
- **Key decisions:** Loopback (RFC 8252) redirect for MCP OAuth rather than Glaze's `OAuthService` relay, because MCP needs dynamic client registration the high-level service doesn't model. "Local" is a static execution badge (per user). Skills = SKILL.md-subfolder convention.
- **UI elements:** composer breadcrumb (folder/Local/branch), branch-picker dropdown, MCP Reload/Authorize buttons + OAuth switch, discovered-skills list.
- **Backend elements:** git_cli, filesystem skill discovery, ipc_handlers, oauth (loopback + safeStorage), mcp reconnect.
- **Verified live:** composer renders folder `godot-game-test` · `Local` · `main`; branch picker opens with real "Uncommitted: 14 files" + Create action. type-check/lint/build all green.
- **Untested:** the MCP OAuth handshake against a live provider (no Composio OAuth account available) — structure follows the MCP spec but the end-to-end token exchange wasn't exercised. Composio's `connect.composio.dev/mcp` also works today via the `x-consumer-api-key` header without OAuth.

### 2026-07-17 — Redesigned model picker as searchable combobox (icons, quant, pin)

- **Goal:** Match a reference design — compact composer trigger + a searchable dropdown with per-model icons, quant tags, active check, and pin-to-top.
- **What was done:** Rewrote `renderer/components/model-picker.tsx` from a grouped `Select` to `CustomDropdownMenu` + `Command` (cmdk). Trigger: Cloud/Cpu icon + label + `ChevronsUpDown`. Dropdown: "Model" header, filter input, flat hosted-first list; each row = provider-type icon + label + parsed quant/format tag + `Check` (active) + hover pin button. Added pinning persisted in `aiden-agent.pinnedModels` (pinned float to top). Kept `encodeSelection`/`decodeSelection`/`isUsable` exports + the `ModelPicker` props unchanged (chat-pane + use-model-selection untouched).
- **Key decisions:** No `Popover` component exists in the SDK, so used `CustomDropdownMenu` (non-modal) as the anchored shell around cmdk. `Command onKeyDown` stops propagation except Escape so cmdk owns arrow/typeahead nav while the menu still closes. Local vs hosted detected via `!needsKey` or localhost baseUrl. Quant tag parsed from trailing token (MLX/GGUF/Q#_…); hosted models simply show none.
- **UI elements:** composer model-picker trigger, searchable dropdown, model rows w/ icons + quant + check + pin.
- **Backend elements:** none (frontend-only).
- **Corrections/Lessons:** Verified live — trigger shows Cloud + "deepseek-chat"; dropdown filter works (typed "reason" → only deepseek-reasoner); active item shows single check; pin button present. Two `chevrons-up-down` triggers exist (workspace switcher + model picker) — disambiguate by the Cloud/Cpu icon.
- **User Frustrations & Important Remarks:** Purely a visual/UX request via screenshots; delivered the combobox look incl. pinning.

### 2026-07-17 — Pivot on-device voice to Parakeet (sherpa-onnx) + fix root wiring bug

- **Goal:** User reported on-device "didn't work" (mic errored `voice:transcribe` = OpenAI/no key) and wanted a full handy-style model-management subview. Chose **Parakeet only, no Whisper**.
- **Root cause found:** `settings:set` in `main/handlers/providers.ts` is a strict whitelist — it silently dropped `voiceProvider:"local"` and never listed `localVoiceModel`/`dictationEnabled`/`dictationAccelerator`. So selecting On-device never persisted → mic always used cloud. Fixed by whitelisting all of them. **Lesson: any new AppSettings key must be added to the `settings:set` whitelist or it's dropped.**
- **What was done:** Swapped engine whisper.cpp → **sherpa-onnx-node** (bundled native addon, no Homebrew/install). Added dep + `glaze.config.ts` `externalizePackage`. New `parakeet.ts` (OfflineRecognizer, `nemo_transducer`), rewrote `local-models.ts` for Parakeet tar.bz2 catalog (v3 25-lang recommended, v2 English) from k2-fsa releases → download+`/usr/bin/tar` extract to `userData/parakeet-models/`. Handlers now `localVoice:status`(ready/error) + `voice:transcribeLocal(pcmBase64)`. Recorder sends 16 kHz Float32 PCM. New `model-manager-view.tsx` subview (search, Downloaded/Available, accuracy/speed meters, badges, download/cancel/delete/use); `local-voice-settings.tsx` reworked to engine-status + Manage-Models button + dictation. Removed `whisper-cli.ts`.
- **Key decisions:** Parakeet needs ONNX (whisper.cpp can't run it) → sherpa-onnx-node is the bundled runtime; huge UX win = no external install. Activating a model auto-sets `voiceProvider:"local"`; first download auto-activates. Models are directory-based (encoder/decoder/joiner int8 onnx + tokens.txt).
- **UI elements:** provider select (On-device (Parakeet)), engine Ready/Callout, Manage Models subview w/ model cards + meters + progress, dictation switch + hotkey recorder.
- **Backend elements:** native module bundling (externalizePackage), sherpa-onnx OfflineRecognizer, streaming download + tar extraction, PCM IPC, settings whitelist fix.
- **Corrections/Lessons:** Verified the native engine loads two ways — `node -e require()` on the built bundle (OfflineRecognizer present) AND live in-app engine badge shows **Ready**. `externalizePackage` correctly copied `sherpa-onnx.node` + `libonnxruntime`/`libsherpa-onnx-c-api` dylibs. `settings:set` whitelist was the silent-drop trap. DataStore caches config in memory (edits need a backend restart). Confirmed live: provider persists to local, engine Ready, model manager renders v2/v3 cards. Not testable here: actual 600 MB model download (needs network) + transcription accuracy.
- **User Frustrations & Important Remarks:** User was frustrated the first version didn't work; the whitelist drop was the culprit. Wanted handy's exact models — clarified Parakeet/Nemotron need ONNX (delivered Parakeet); Whisper explicitly dropped per user.

### 2026-07-17 — On-device (local) voice transcription + dictation hotkey

- **Goal:** Add local voice transcription to Voice settings (inspired by handy): download/manage Whisper models locally, run transcription on-device, and a configurable hotkey for local dictation.
- **What was done:** New provider `"local"` in Voice settings. Backend: `whisper-cli.ts` (locate/install whisper.cpp via Homebrew, `transcribeLocal`), `local-models.ts` (GGML catalog from ggerganov/whisper.cpp HF repo → `userData/whisper-models/`, streaming download w/ progress + cancel + delete), `local-voice.ts` handlers. Settings gained `voiceProvider:"local"`, `localVoiceModel`, `dictationEnabled`, `dictationAccelerator`. `shortcut.ts` now registers a 2nd (dictation) hotkey → broadcasts `app:dictate-toggle`. Frontend: `local-voice-settings.tsx` (engine status/install w/ live log, model manager list w/ progress bars, active-model select, dictation recorder), recorder decodes to 16 kHz WAV for local, composer passes provider/model + listens for `app:dictate-toggle`, shared `accelerator.ts` helper.
- **Key decisions:** Chose whisper.cpp CLI (fast, Metal, matches handy) over bundled transformers.js, per user. Dictation hotkey reuses the composer recorder with the *selected* provider (not a separate local-only path). Audio decode/resample happens in the renderer (Web Audio) so no ffmpeg dep — backend only needs whisper-cli. Models download to `.part` then rename so listed models are always whole. Binaries resolved from Homebrew bin dirs directly (GUI apps lack shell PATH).
- **UI elements:** Voice provider select (+On-device), engine install button + log well, model download/delete list w/ custom progress bar + Badge, active-model Select, dictation enable Switch + hotkey recorder.
- **Backend elements:** child_process (spawn brew install, execFile whisper-cli), streaming fetch download, global shortcut, ipc_handler, local file storage.
- **Corrections/Lessons:** Re-confirmed `window.glazeAPI.glaze.ipc.invoke` fails from LiveAppEvaluate (WKContentWorld isolation → "GlazeIPCError 0"); and Radix Select portals can't be opened via synthetic pointer/keyboard events — so the local panel's live render couldn't be auto-exercised (cloud Voice path + new copy verified in DOM; build/type-check/lint green). Homebrew formula is `whisper-cpp`, binary is `whisper-cli`.
- **User Frustrations & Important Remarks:** User referenced `/Users/sambitbiswas/projects/opp/handy` (Tauri/Rust whisper.cpp app) as the inspiration; wanted download+manage models locally + hotkey for local transcription. Install/download flows require the user's Homebrew + network (expected for a real on-device feature).

### 2026-07-17 — Composer solid bg + file/image attachments + models.dev capability catalog

- **Goal:** (1) Composer had no background so transcript text bled/clipped through it — make it a solid color. (2) The composer `+` should attach files/images (capability-dependent). (3) In settings, cross-check every model against models.dev and enrich with vision/tool-calling/context/open-source/reasoning.
- **What was done:** (1) Wrapped the composer in a full-width opaque `bg-background` + `border-t` bar (verified `bg-background`=opaque white; `bg-well`/footer were transparent). (2) Attachments: `attachments.ts` (read images→base64 / text→inline), `attachments:read` IPC, `pickFiles` (native multi-file dialog), composer `+` → chips (image thumbs/file chips, remove) + vision gating (drops images w/ toast on non-vision models); `Attachment` on `ChatMessage`/`ChatStartParams`; persisted + rendered in transcript; `llm-client.toPiMessages` builds pi user content blocks (text inlined, images as `{type:"image"}` only when model vision-capable). (3) `models-catalog.ts` fetches+caches models.dev api.json, maps provider+model → normalized `ModelInfo`; `models:info`/`models:refresh` IPC; `useModelInfo` hook; capability **badges per model in the provider editor**; vision flag gates attachments.
- **Key decisions:** New-chat moved out of the composer (`+` is now attach); it stays in the header (`SquarePen`). Capability badges live in the provider editor, NOT the model picker — Radix `SelectItemText` isn't exported by @glaze/core, so inline item badges leaked into the trigger ("deepseek-chat1M"); reverted the picker to plain names. Image attachments only sent to vision models (models.dev `attachment`/modalities). Catalog cached 24h in userData; lazy-fetched on first `models:info`.
- **UI elements:** opaque composer bar, attachment chips + thumbnails, provider-editor capabilities list (Badge: Vision/Tools/Reasoning/Open/context).
- **Backend elements:** models.dev catalog service + cache, attachment reader, pi image/text content conversion, IPC handlers.
- **Corrections/Lessons:** `window.glazeAPI` isn't reachable from LiveAppEvaluate (isolated WKContentWorld) — validate IPC-backed features via the UI, not evaluate. NUL-byte literal got mangled in an edit → used `buf.includes(0)` for the binary check. WebFetch of models.dev returned a fuzzed/hallucinated snapshot (bogus provider names) but the real field schema (attachment/reasoning/tool_call/modalities/limit/open_weights) is correct; the app fetches the true JSON at runtime (confirmed: deepseek-chat matched w/ context). Radix Select trigger mirrors the selected item's full children.
- **User Frustrations & Important Remarks:** The original composer clipping bug was visible in the user's screenshot (chat text under the composer). Model-capability cross-check was requested specifically "in settings" — provider editor is the home.

### 2026-07-17 — Workspaces + Pi coding agent (folder tools, per-workspace permissions, approvals) + drop Vercel AI SDK

- **Goal:** Go Pi-only; let users make folder-bound **workspaces** for their chats; give Pi real coding powers in the folder with per-workspace permissions (Full/Ask/None); redesign the composer to match a reference (folder + git-branch pills, "Do anything", permission control, model picker); switch folders from the left sidebar. User confirmed: full coding agent, full interactive approval, workspace-has-many-chats.
- **What was done:** Removed `ai`/`@ai-sdk/*` (were already unused in code). Backend — `Workspace` model + config-store CRUD/seed; `chat-store` workspaceId + list filter + legacy backfill to `"default"`; `git.ts` (branch detection); `coding-tools.ts` (read/write/edit/list/glob/grep/run_command, root-confined); `tools.ts` `buildAgentTools({workspaceRoot,permission})`; `llm-client.ts` workspace context + dynamic system prompt + `beforeToolCall` approval flow + `approve()`; `handlers/workspaces.ts` + `chat:approve` + workspaceId plumbing. Frontend — `workspace-context.tsx` (active workspace, `aiden-agent.workspaceId`); workspaces/git queries + `pickFolder`; sidebar workspace switcher (`DropdownMenu`); composer redesign (pills, permission menu, model picker moved here, "Do anything"); chat-pane inline Allow/Deny approval cards + workspace-aware generation.
- **Key decisions:** Hand-built coding tools as pi `AgentTool`s (kept control; didn't adopt pi's heavier harness). Approval = pi `beforeToolCall` returns a Promise resolved by renderer `chat:approve`, or `{block:true}` on deny/abort; only write/edit/run_command gate in "ask" (read-only tools auto-run). Active workspace shared via React context (sidebar + pane need the same value). Folder reveal via backend `shell.openPath` (no preload change); folder picker via already-exposed `dialog.showOpenDialog`.
- **UI elements:** sidebar workspace switcher, redesigned composer (folder/git pills, permission dropdown, model picker), inline approval card, workspace-remove AlertDialog.
- **Backend elements:** workspace store, git detection, folder-scoped coding tools, pi approval hook, ipc handlers, shell.openPath.
- **Corrections/Lessons:** pi `beforeToolCall` is the clean pause point (async, can `block`, honors abort signal); `context.toolCall.name` + `context.args` drive the approval summary. `fs.glob` available on Node 24 (no dep). Type-check + lint clean; build pruned the AI SDK deps.
- **User Frustrations & Important Remarks:** "Ignore the local for now" — the reference's "Local" pill was deliberately NOT built. Default workspace is folderless (folder pill inert, no git pill) until a folder-bound workspace is created; couldn't e2e-test folder tools/git/approval end-to-end without a real folder + generation, but UI + wiring validated live.

### 2026-07-17 — Replace engine with pi agent loop + UI refit (grouped sidebar, header model picker)

- **Goal:** (1) Match the reference chat UI more closely; (2) run the pi agent loop (github.com/earendil-works/pi) as the generation engine. User chose: replace the chat engine with pi, embedded in-process (not the CLI).
- **What was done:** Installed `@earendil-works/pi-agent-core` + `pi-ai`. Rewrote `llm-client.ts` to run `new Agent(...).continue()` — builds a `Model<Api>` (with provider `baseUrl` for custom endpoints), a `streamFn` over `openAICompletionsApi()`/`anthropicMessagesApi()`, `getApiKey`, and `buildAgentTools()`; keeps the same `start/cancel` interface + `chat:delta/tool/done/error` broadcasts. Ported Exa/Skills/MCP tools to pi `AgentTool[]` (typebox schemas; MCP via `Type.Unsafe`). UI: sidebar chat history grouped into time buckets (`SidebarListGroup`); moved the model picker into the `ScrollArea` header `subtitle`; added new-chat + settings glass icon buttons to header `actions`; composer got a leading `Plus` new-chat button and the "Ask anything, @ tools, or / for commands…" placeholder.
- **Key decisions:** Embed pi libs in-process (no background CLI). Keep the existing broadcast/IPC contract so handlers + renderer stayed unchanged. Left Vercel AI SDK installed-but-unused rather than risk removal.
- **UI elements:** grouped sidebar, chat header (title + model picker subtitle + icon actions), redesigned composer.
- **Backend elements:** pi Agent loop, pi-ai provider streams, typebox tool schemas, MCP→pi tool adapter.
- **Corrections/Lessons:** pi requires Node ≥22.19 (have 24). pi tool result `content` items need literal `type:"text"` — annotate the helper's return type so the literal is contextually typed. `Agent.continue()` resolves on both done and abort; errors arrive as an `assistantMessageEvent` of type `"error"`. Verified live: a real DeepSeek generation streamed correctly through pi.
- **User Frustrations & Important Remarks:** Screenshot showed an "Archive" item at the sidebar bottom — deliberately NOT added (no archive feature exists; would be dead UI). Offer to build real chat archiving if wanted.

### 2026-07-17 — Phase 2: tools (MCP + Skills + Exa), voice, global shortcut, collapsible sidebar

- **Goal:** Add MCP servers, Agent Skills, Exa web search (with tool-calling), voice input (OpenAI/Gemini), a configurable global shortcut (default ⌘⌥Space), and make the sidebar collapsible.
- **What was done:** Backend — `tools.ts` (Exa + skills + MCP → ToolSet), `mcp.ts` (McpManager via official SDK), rewrote `llm-client.ts` for multi-step tool calling over `fullStream` with `chat:tool` events, `transcription.ts`, `shortcut.ts`; extended config-store (mcpServers/skills) + settings; `phase2.ts` handlers. Renderer — 5 new settings sections + editors, composer mic button + `use-voice-recorder`, `SplitView.SidebarToggle`, tool-status line, `app:focus-composer` listener. Declared `microphone` capability.
- **Key decisions:** Skills = Agent Skills (instruction docs as tools). Shortcut focuses app+composer (no HUD). MCP via `@modelcontextprotocol/sdk` (ai v5 dropped `experimental_createMCPClient`). Tools enabled globally via settings toggles.
- **UI elements:** settings sidebar (7 sections), MCP/skill editor dialogs, composer voice button, sidebar collapse toggle, tool-activity status.
- **Backend elements:** MCP client manager (child_process/http), AI-SDK tool calling, safeStorage (Exa key), globalShortcut, audio transcription APIs.
- **Corrections/Lessons:** **zod in AI SDK `tool()` OOM'd tsc** → switched all tool inputs to `jsonSchema`, dropped zod. `experimental_createMCPClient` not in `ai` v5 → used official MCP SDK directly. Live-inspection: bare `.click()` didn't trigger SidebarListItem nav — needed full pointer-event sequence (real clicks fine); React state reads need a tick after clicking.
- **User Frustrations & Important Remarks:** None. Voice reuses provider keys; mic permission prompt on first use.

### 2026-07-17 — Build v1 core AI chat client

- **Goal:** Multi-provider local/hosted AI chat with history sidebar, encrypted keys, light/dark, in-app settings. (MCP/Skills/Exa deferred to phase 2.)
- **What was done:** Built backend services (secrets, config+presets, chat store, AI-SDK streaming client, model listing) + IPC handlers; renderer chat shell (SplitView + sidebar), streaming transcript with markdown, composer + grouped model picker, provider settings (key entry, test, model refresh) + appearance settings. Seeded 7 preset providers.
- **Key decisions:** Vercel AI SDK over hand-rolled SSE (unifies OpenAI/Anthropic + sets up phase-2 tool-calling). Backend-owned everything. Client-generated streamId to avoid dropped opening tokens. `/` redirects to a concrete chat.
- **UI elements:** sidebar (chat history), transcript/ScrollArea, composer, model Select, settings split-view, provider dialog, rename/delete dialogs.
- **Backend elements:** safeStorage secrets, JSON file storage, streaming IPC (broadcast/notification), external LLM APIs.
- **Corrections/Lessons:** `ModelsResponse.data` needed shared element type with `models` (TS union access). Removed Lucide icon from EmptyState media per design guidance. Removed template `home-view.tsx`.
- **User Frustrations & Important Remarks:** None. User chose "solid core first, tools next"; skills defined as tool-like (→ phase 2); in-app full-screen settings.
