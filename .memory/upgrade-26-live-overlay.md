# Lane 26 — Live launcher and floating Environment clearance

- Baseline `5cc831a17aa8971fb5b89c7dd9278a6ec4022beb`: default floating Environment ends 12px above the window bottom while the 56px Live launcher starts 16px above the bottom. The launcher intercepts the browser annotation Add to chat action. Reproduced with ordinary Playwright pointer activation on unmodified product code; lane18 independently reproduced the same failure.
- Reserve space below floating Environment only while the Live dock is mounted. Shared CSS variables retain the launcher's existing 3.5rem size and 1rem edge inset; clearance adds the panel's existing three-spacing gap. Inline Environment retains its horizontal dock inset. No colors, motion, shape, focus behavior, provider calls, runtime contracts, or onboarding capabilities change.
- Keep the launcher reachable: moving it horizontally beside floating tools would fail at the minimum 390px window width. Raising the annotation z-index or disabling launcher pointer events would only exchange the blocked control.
- Existing registered `tests/e2e/browser.spec.ts` now covers actual pointer clicks at 1280x800, 390x456 with reduced motion, 900x600, and 1600x900 inline. It checks launcher pointer and Enter activation, expanded setup dismissal, annotation Enter activation and successful Meta+Enter delivery. Compact-width testing must reopen the intentionally collapsed sidebar before continuing to Settings.
- Isolated Electron profile only; no microphone/provider/native Live session acceptance is claimed. This is a desktop layout correction with no native-client or onboarding contract change.
- Validation: build, production/E2E type checks, focused ESLint, 140 browser unit tests, 62 assistant/environment unit tests, and all 15 scoped Electron scenarios passed (strict flaky gate; 2.2 minutes). Baseline failure and before/after captures: `/tmp/aiden-26-baseline.log`, `/tmp/aiden-26-evidence/`. Hosted CI and central review remain separate gates.
