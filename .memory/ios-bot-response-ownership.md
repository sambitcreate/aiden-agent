# iOS Bot response ownership

Isolated worktree /Users/sambitbiswas/.codex/worktrees/ios-bot-response-ownership/aiden-agent, branch feature/ios-bot-response-ownership atop PR217 ff777f501. PR217/225/227 heads remain unchanged. PR227 exact3f7154e7 all hosted CI green,0threads, acceptance recorded.

Bot open/hydrate reserve cache tickets before their first await and share a navigation attempt owner. Later loads and away/back scope changes invalidate older callbacks; deep-link publication supersedes them. Permission completion updates the currently presented transcript instead of its captured pre-permission copy. Cache admission retains an in-memory winner before disk IO, with removal/purge fences, so an old rejected GET cannot fall back to stale disk after a newer write failure. Warm Bot loads also use the admitted winner.

New held production HTTP regression covers GET A delayed behind GET B, newer disk failure, removal and purge, with request-origin admission and persisted winner checks. A held permission-attempt regression covers away/back and instance/device navigation. Generic XCTest compilation passes; physical execution is not claimed. Android/policy checks and independent Sol medium reviews underway. These tests are compiled, not executed baseline reproductions; original-main duplicateGET reachability was source-confirmed in .memory/mobile-response-ownership.md. iOS workspace rename/list ownership remains assigned separately; physical device locked, no simulator/device launch.

Review corrections: path mutations now invalidate attempts synchronously through the same production helper used with the real navigation store in the held away/back test; error continuations also directly check path/scope. Live installation write grants and a live notice-policy closure are rechecked at permission publication against the attempt policy. Restoration identity includes both policy inputs, allowing current-chat permission refresh without transcript rollback. Deep-link permission and detail transcript callbacks preserve the other current field. The admitted cache API rejects disk fallback after purge even if cleanup left a file; fresh post-purge admission remains valid. Four XCTest methods compile, covering held production GET order/disk/removal/purge, real navigation-store away/back ownership, and held write/notice downgrades. Android177 JVM/lint/instrumentation and policy31Node20Ruby42 pass. Final independent reviews/build pending; no physical execution claimed.

Create review correction: a successful Bot create receipt publishes the shared awaitingPermission presentation (mutations false), then existing-destination restoration resolves live permission. Added a held real Bot-create POST regression using that same production presentation. This avoids inheriting pre-POST permission across a downgrade without retrying the POST. Fixed the test idempotency argument to UUID after a compilation diagnostic. Final generic XCTest build is being confirmed; both actual-diff re-reviews requested.

Final acceptance: generic iOS app/XCTest build-for-testing passes (/tmp/aiden-ios-bot-response-build.log); Android177 JVM/lint/instrumentation compilation and policy31Node20Ruby42 pass (/tmp/aiden-ios-bot-response-android.log and /tmp/aiden-ios-bot-response-policy.log). Both independent GPT-5.6 Sol medium final actual-diff reviews are clear. Physical XCTest remains unexecuted.
