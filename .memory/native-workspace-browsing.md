# Native workspace browsing — 2026-09-22

Baseline `c8c09e0d2` (fresh origin/main); branch `feature/native-workspace-browsing`. No existing PR covered this scope. Notion Hermex research was dated 2026-09-20; verified GitHub #401 (`6c63839705e6`), #400 (`f7841fa74570`), #402 (`34d176414b17`) and read pinned FileTree.swift. Original Aiden implementation; no upstream source copied. Bot controls, streaming, produced-file tools, Git viewers, image loading, and write authority expansion are excluded.

## Implementation

- `files?tree=1` adds optional 200-entry direct-child pages with opaque directory handles and snapshot cursors; no client Mac paths. Legacy no-query recursive snapshot stays compatible. Scan: 8,000 records/4,000 entries/depth 20; no recursive indexing. 16 unfinished inventories with oldest eviction, ten-minute cursor lifetime, device/workspace/revision/identity fencing, fixed memory bounds. Generated folders remain excluded. Directory symlinks are not offered by lazy browsing.
- Both native clients expand/collapse downloaded folders, search loaded entries with ancestor context, load continuation pages explicitly, and cap accumulated entries at 4,000. Cached snapshots stay read-only. Native source preview is numbered plain text, capped to 2,000 lines and 2,000 characters/line with clipping notice; explicit Edit retains bounded existing reads and expected-version saves.
- Explicit `./` Markdown references resolve at most 40 directory pages through opaque handles before reading. Traversal, absolute/home paths and unsafe encodings are rejected; no automatic image networking. Android formatter now makes those links tappable. iOS preserves system URL actions including app-settings.
- Normative API, OpenAPI, shared/Android fixture, native fixture decoding tests, existing onboarding file tile, and iOS onboarding copy updated. Existing artwork is reused because this refines the shipped Files capability.

## Reviews and evidence

Separate user-requested GPT-5.6 Sol medium reviews covered blast radius and adversarial cases. Fixed discovered stale linked-file selection writes, Android stale root failures, missing Android page byte cap, non-clickable Android Markdown links, global snapshot-capacity denial, depth-boundary mismatch, offline fallback state, and inherited iOS Settings URL suppression. Coordinator required closing the residual metadata boundary: a local actor with workspace rename/symlink rights could expose outside directory cardinality through a timed swap-back. Lazy enumeration now uses an additive read-only operation in the existing packaged worktree-file-io helper: root/target inode checks, component-by-component openat(O_NOFOLLOW), fdopendir/readdir on the held descriptor, and no-follow child metadata. Deterministic native checkpoints swap to an outside directory during enumeration and back before completion; only the original directory names/count return. Non-Mac hosts return 404 for the opt-in route and native clients retain the legacy snapshot fallback. Both Sol reviewers cleared the additive native helper and wrapper. Four native descriptor/copy/restore tests and 58 file/router regressions pass; TypeScript and scoped ESLint pass. The wrapper can resolve/stat a rejected symlink target while capturing identity, as existing opaque identity checks do, but it never enumerates or projects that target.

Validation: 94 focused server/contract/Bot/onboarding tests passed; 180 Android unit tests and Android lint passed, including fixture and clickable link tests. Final generic iOS device-platform app/test compilation, TypeScript, scoped ESLint and whitespace checks passed. Both Sol reviewers confirmed no remaining must-fix finding after remediation. Hosted checks and coordinated physical XCTest remain pending. React Doctor 91/100: only serial-await warning in bounded file identity issuance; intentionally sequential for cancellation and bounded descriptor pressure.

One simulator test was mistakenly executed before reading nested ios/AGENTS.md. It is excluded from acceptance; see papercut. No simulator work will be repeated. No merge/release/deploy authorized or performed.

## Pullfrog follow-up — 2026-09-22

Four actionable review threads addressed: lazy root substitution, overlapping Android initial refresh, stale offline edit authority, and relative links bypassing the offline document cache. Lazy admission rejects a symlink at the configured root and pins the first admitted canonical root/device/inode. The authenticated directory and pinned root identities pass unchanged into the native helper; replacement tests cover both boundaries. This is first-use pinning, not registration-time pinning (the persisted workspace has no inode); legacy list/read/write retains existing behavior. The API documents this scope. Adversarial Sol review cleared the final lazy path.

Both native clients persist link downloads, find cached relative references by safe path within the paired instance/workspace, retain documents across partial lazy index refreshes, and keep document edit authority separate from folder paging availability. Android initial refresh is serialized. Focused cache and authority regressions added in both native suites.

Server follow-up: 22 focused tests passed; full test:aiden-remote passed 458 tests with one skip; TypeScript and scoped ESLint passed. Latest-head hosted checks must be rechecked after push; predecessor head 12bcee75 had green Android/release-contract checks and running macOS verify/Electron E2E.

Physical acceptance external blocker confirmed by coordinator: shared iPhone reports passcodeRequired:true, unlockedSinceBoot:true, without subsequent user unlock confirmation. Browser slot is queued behind recovery, Bots, produced-file, worktree and form-fill; no handoff received. No physical or simulator tests launched in this follow-up. Greptile remains externally blocked by its 50-credit trial limit. Heartbeat stays active while newest-head CI/review gates remain pending.

Final follow-up native evidence: 182 Android unit tests pass; generic unsigned iOS device-platform app/test compilation passes (not physical runtime acceptance). Native authority split reviewed by Sol; no further scoped issue found.

## Incremental review follow-up — 2026-09-22 12:24 UTC (uncommitted)

Pullfrog review 5277973359 on c33dc923 opened findings 4071555899 (path-based lazy claims/read-write race), 4071555907 (first-use symlinked ancestor), and 4071555916 (Android disconnect leaves retained client editable). Local changes address all three: enumeration returns file dev/inode directly; lazy claims retain pinned root identity and no longer stat each child path; lazy reads/edits execute within the native helper using no-follow absolute-root/relative-parent traversal and exact file identity. Configured ancestor aliases are rejected except fixed macOS /tmp,/var,/etc mappings. Android observes connection state and requires CONNECTED for editing/saving/paging. Legacy snapshot file behavior remains separate.

Native edits preserve expected content SHA and displaced-inode checks, reserve handle capacity/fresh expiry before mutation, return an updated file handle, and retain recovery copies conservatively. Sol review caught TTL/capacity-after-write and unlimited recovery growth; regression covers capacity rejection without mutation and save across old expiry. Native parent-directory flock and a 16-copy/8,000-scan cap prevent unbounded recovery accumulation; at cap the file is unchanged and the API asks for Mac-side recovery review/removal.

Local evidence: 23 file/opaque identity tests pass; 9 native tests pass (root/ancestor symlinks, stale inode/version, read/save parent swaps, recovery limit); universal native helper compiled. TypeScript/scoped ESLint were green before final TTL/cap refinement, final recheck pending below. Cross-platform contract/router run: 18 passed, 43 blocked by `listen EPERM 127.0.0.1`, not counted green. Android focused rerun blocked before compilation by ~/.gradle wrapper lock EPERM. No new iOS or simulator/device run.

Publication blocked: git add cannot create /Users/sambitbiswas/projects/aiden-agent/.git/worktrees/aiden-agent19/index.lock under current restricted sandbox. GitHub CLI network blocked, but connector read tools work. Coordinator send tool rejected because approval required with never policy; coordinator independently read progress and acknowledged exact restrictions, requested continued permitted local work. No bypass attempted. Three new threads remain unresolved; no claim of a pushed fix. At c33dc923, hosted macOS verify/Android/release contract passed and Electron E2E was still running. Prior locked shared iPhone/queued-slot and Greptile credit blockers remain.

Final local recheck: TypeScript, scoped ESLint, and diff whitespace pass after handle reservation/recovery-cap changes. Both Sol reviewers completed full boundary follow-up and found no remaining path escape or expected-version bypass; native post-install digest is verified through held FD. Residual save-result uncertainty after an OS/output failure can require reopening Files to reconcile, as with existing saves. As of final connector check, c33dc923 Electron E2E remains in progress. Working tree intentionally retains uncommitted fixes pending restored Git/validation access; heartbeat must not mark completion or resolve the three new threads until fixes are published with evidence.

2026-09-22 13:04 UTC heartbeat: connector confirms all CI jobs on published c33dc923645f1acef54ff91b4d32674deac22c5b are now successful, including Electron E2E job 106733251234 (run 35723932581); macOS verify and Android are also green. No new review comments since 12:24. This does not validate the uncommitted descriptor read/edit and Android disconnect fixes. Three incremental review findings remain open until publication. Git/Gradle/socket/coordinator-tool restrictions unchanged; local changes preserved and heartbeat remains active.

13:12 coordinator audit: re-read effective developer permission instructions: workspace-write, restricted network, approval never; Git worktree metadata explicitly read-only. These restrictions are current, not inferred from stale failures. A fresh coordinator send attempt on the audit request again returned `MCP tool call requires approval, but approval policy is never`. Exact prior action failures: git add -> worktree index.lock Operation not permitted; focused Gradle -> ~/.gradle/wrapper/dists/gradle-9.1.0-bin/9agqghryom9wkf8r80qlhnts3/gradle-9.1.0-bin.zip.lck Operation not permitted; router tests -> listen EPERM 127.0.0.1; gh -> cannot connect api.github.com. Connector reads work. No repeated denied Git/Gradle/socket attempt or bypass. New local evidence remains 23 file/opaque tests, 9 native tests, universal native compile, TypeScript/ESLint/diffcheck and both Sol reviews; Android regression not run under current restrictions. Published-head Android/iOS evidence must not be credited to local follow-up.

## Restored-access validation — 2026-09-23

User restored full filesystem/network access. The preserved three incremental Pullfrog fixes are now validated: full remote suite 459 passed/1 skipped (including router/socket coverage), 9 native descriptor race/edit tests passed, TypeScript/scoped ESLint/whitespace passed, and generic unsigned iOS device-platform app/test compilation passed. Android 182 unit tests passed; lint completion is recorded below. Both prior Sol reviews cover this patch. iOS save consumes the returned document and updated handle unchanged.

Coordinator task 01a0c7a8-3a51-76a3-996d-77b2cd3e0e1c confirmed the user explicitly waived physical iOS testing. Physical acceptance is WAIVED, not passed; no physical tests or simulators launched. Earlier device-slot blocker is historical. Greptile remains externally blocked by its trial limit. New-head CI/Pullfrog will be checked after publication; predecessor CI is not credited to this patch.

Android lint completed successfully after restored access. All affected local checks are green; publication and newest-head hosted checks follow.

## CI transfer-alias correction — 2026-09-23

Published f90d42dd4 and resolved incremental review threads. Hosted verify run 35878243333 failed in dirty-removal snapshot capture: persisted worktree identity paths can use /var aliases, but the strengthened native root walker rejects symlink ancestors. Reproduced locally. Transfer(copy/restore) wrapper now canonicalizes identity paths while preserving saved dev/inode authority; missing aliases return typed unsafe-source/destination errors. Lazy configured-ancestor admission remains unchanged. Worktree owner confirmed the persisted-path contract read-only. Both Sol reviewers cleared the fix.

104 service-boundary tests and 10 native tests pass, including aliased copy/restore, replaced root refusal, removed alias typing and substitutions between canonicalization/native opening. TypeScript/scoped ESLint/diffcheck pass. Extra lifecycle suite initially 21/22: unrelated capacity test compared free disk bytes across two live samples while builds ran; focused recheck passed. No capacity implementation changed. Physical iOS testing remains waived; new-head CI/review follows publication.

The broader lifecycle rerun reproduced the same baseline statfs equality flake. Worktree and recovery owners confirmed the existing test-only fix; ported the exact final capacity predicate/regression from recovery commit 0cba92f10675eead1b11d1a056aa7365487373f8 without production changes. All 23 lifecycle tests and ESLint pass after the correction. Both reviewers checked this scoped test change. Transfer alias correction is separate commit 5e4849ef.
