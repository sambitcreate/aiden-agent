# Quick View menu focus handoff — 2026-09-19

- Baseline `5cc831a1`: Electron at 900px reproduces Quick View > Browse files opening tools, then menu return focus reactivating Quick View and making tools inert. Removed-trigger fallback and nested Escape dismissal pass baseline.
- `QuickViewCard` defers the chosen destination until menu `onCloseAutoFocus`, consumes the destination once, and focuses its durable trigger before opening Files/Review. Ordinary dismissal retains Radix return focus. Closed, covered, busy, or unmounted surfaces discard pending navigation. Git-busy menu entries are disabled.
- Read-only reference study: Waku `src/app/image_preview.rs` guards deferred focus by overlay generation and restores a previous target/fallback; OpenCode v2 `packages/app/src/components/dialog-select-model.tsx` distinguishes selection/Escape/outside dismissal in `onCloseAutoFocus`. Original Aiden implementation; no source copied.
- `tests/e2e/environment-focus.spec.ts` is discovered automatically by the existing Playwright `**/*.spec.ts` match and `npm run test:e2e`. Covers three menu destinations, pointer/keyboard, stacked/alongside layouts, normal/reduced motion, Escape, disappearing trigger, held Git operation, and Settings unmount.
- Desktop-local focus/menu behavior only; no server or native transcript/activity contract changes, onboarding capability inventory changes, or visual redesign.
- Final local verification: 7 Electron cases passed in 59.8s without retries (six new cases plus existing chat-shell keyboard case), 35 focused Environment tests passed, both TypeScript checks and scoped ESLint passed. Production build and final renderer build passed. Hosted CI and independent review remain separate gates.
