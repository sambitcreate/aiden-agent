# Mobile Bot cockpit — 2026-09-22

Baseline audited at origin/main `c8c09e0d2`, not the coordinator's older checkout. Native task progress and bounded read-only agent roster/detail already ship. Remote v1 supports device-owned parent cancellation and one-time allow/deny approvals; it does not yet define Queue/Steer, mobile clarification/credential requests, or child interruption.

Hermex evidence verified: `uzairansaruzi/hermex` PRs #503–508 and #580 (bot work progress, pending requests, explicit run input, live inbox, artifacts, settings, delegated work). Especially #504 binds request identity at dispatch and does not resend uncertain answers; #505 distinguishes admission from consumption and preserves drafts. Adapt those invariants to Aiden; do not import Hermes WebSocket/RPC authority or secret collection. Source plans: Notion pages `3e280314a1c481fbaafecf213cb69f72` and `3e280314a1c481e7ad47cbd668968a42`, dated 2026-09-21, are research rather than current implementation truth.

This slice binds approval taps to the displayed ID, serializes decisions/Stop, replaces captured-card restoration with authoritative snapshot reads, fences late results against connection/stream/terminal state, preserves legacy Workspace Stop, and checks Bot read/write grants. Stop remains active until host acknowledgement; rejected/mismatched responses surface uncertainty. Android disables OkHttp connection retries for these two writes after a real socket-disconnect fixture reproduced four POSTs. Other HTTP requests and generation recovery are unchanged.

Tests extend the existing XCTest/JUnit files already discovered by native suites. Two independent GPT-5.6 Sol medium reviews found legacy support, stale capability-card, missing chat classification, identity-acknowledgement and terminal race issues; fixes and held-response regressions are included. iOS physical XCTest remains blocked by the coordinator's locked iPhone (`com.apple.dt.deviceprep Code=-3`), and compilation is not execution evidence.

Onboarding/bento reviewed: this slice repairs existing advertised controls, changes no first-run setup or permissions, and adds no durable advertised capability. No tour/asset changes are needed. Queue/Steer remains assigned follow-on work with the runtime peer: require a host-owned persistent admission contract and negotiated support before native consumers. Mobile pending questions and child interrupts remain unimplemented, not silently advertised.

## Queue/Steer consumers

Host contract `4e5c42fb` / PR #220 now defines `chat-run-input-v1` and `POST /streams/{streamId}/inputs`. Both native Bots composers gate controls by that advertised feature, exact active stream, current grants, plain text, and 16 KiB UTF-8 limit. Receipts must match request UUID, stream, and mode, with strict accepted/rejected shapes; `admission: queued` means durable host admission, not model consumption. Android control transport retries are disabled.

A protected device-local marker is written before dispatch beside the existing text draft. Unknown results keep both; reopening/restarting cannot silently fall back to Send. The user explicitly reviews the chat before clearing uncertainty. Accepted receipts consume only the captured draft, preserve newer edits, and retain the marker on persistence failure. Two Sol medium reviews found/fixed initial hydration, pre-dispatch edit, failed deletion, and receipt-shape issues. Shared fixture coverage and held-network native tests exercise receipt identity, unsupported feature, stale stream, duplicate taps, accepted/rejected/unknown outcomes and restart persistence.

Native onboarding and the existing Bots bento copy introduce the supported controls; the existing dedicated Bots illustration remains appropriate. No secret/clarification collection or child interrupt endpoint was added. Existing agent inspection stays bounded/read-only. Recovery PR #217 independently adds `isRestoringStream`/active-owner Send gates; integration must preserve those alongside this slice's uncertainty/submission guards.

PR #218 follow-up: Pullfrog found mismatched approval A's response could refresh and hide newer card B when its read failed. Swift now matches Android's no-current-card precondition for that refresh. A held-response/new-card/failed-read XCTest regression is added. Hosted `verify` also exposed the existing shipping source test's outdated exact `.disabled` assertion; updated it to require the stronger connection/read-only/in-flight gates. All 31 shipping policy checks pass; physical XCTest remains blocked.


## Combined integration review

Validated the consumer with recovery PR #217 at `7e3c559c` and runtime PR #220 at `ff6c2927`, including PR #218 at `d9974d4b`, in `/tmp/aiden-bot-integration`. Merge resolution preserves both recovery ownership and unconfirmed-input Send guards. Independent Sol reviews found two additional boundaries: another live chat view can hold stale marker state, and an accepted admission can arrive after terminal reconciliation. Send now reads the shared per-chat marker before dispatch and explains blocked cross-view submissions; stale draft generation alone does not block Send. Accepted admission refreshes the authoritative transcript; a failed refresh leaves an explicit queued-but-stale notice with a reopen instruction. No automatic retry of the instruction occurs.

Regression coverage includes accepted/unknown admission after terminal settlement, failed post-receipt transcript reads, cold recovery with a durable uncertainty marker, and old-view marker gating. Both native clients retain their draft/authority fences. Combined Android JVM/lint and unsigned iOS test compilation are rerun after review fixes; device execution remains blocked by the locked physical iPhone. Consumer PR is stacked on #218; operational dependency is #220, and #217 must retain its recovery guards when merged.

PR #218 second review follow-up: overlapping approval snapshot reads now use a monotonic generation on both clients, after validating current context/stream. Success and failure publication require latest generation plus the existing card/state tuple. Held older-first/newer-second tests include an intervening stale-stream invocation. Removing the generation check makes the Android regression fail; restoring it passes. Hosted verify disk-capacity assertion flake is tracked by the coordinator as an unrelated test-only fix.

PR #223 review follow-up: the standalone consumer now fences Android receipt-triggered chat reads by both client and paired instance/device before publication (the combined recovery branch already fenced the client). Held pairing-switch regression verifies neither transcript nor cache is overwritten. Copied OpenAPI metadata is synchronized with latest #220 nonblank pattern and UTF-8-byte extension, with the frozen route inventory updated. Focused native tests and all 15 standalone protocol tests pass; both Sol reviewers clear.

PR #223 indirect refresh follow-up: pending-title retries spawned by a confirmed receipt check the bound client and active instance/device before request and before publication. Held title-pending/pairing-switch coverage preserves both transcript and old-instance cache. The fixture must omit false titlePending (the wire contract only allows true or absence); corrected fixture, focused Android suite passes, both Sol reviews clear.

PR #218 discussion 4071945962: ambiguous response fallback now reuses a current in-flight approval snapshot only when stream, client/context, captured card, and captured stream state still match. This preserves a held authoritative B read while A fails, but admits a fresh fallback when the existing read was captured before answering A. Both native held-order regressions pass/compile; both Sol reviews clear. Focused Android tests pass and generic unsigned iOS build-for-testing succeeds; physical XCTest remains unexecuted.

PR #223 Pullfrog follow-up (b4fc1e1b): an ambiguous-response fallback that coalesces with a matching in-flight approval read now awaits that read's outcome (Android `CompletableDeferred`, iOS `AidenApprovalSnapshotOutcome`) instead of returning. It stops only when the read published the Mac's snapshot; on failure, cancellation, or supersession it re-evaluates and issues its own read. iOS settles/clears the marker right after publishing, before awaiting Live Activity updates, so a response answered during that await cannot coalesce with an already-published read. Android held failure/cancel regressions fail without the fix; iOS equivalents compile (generic unsigned build-for-testing), physical XCTest unexecuted. The post-publication iOS window has no XCTest because `AidenRemoteLiveActivityManager` is a concrete class with no hold seam.
