# Browser crash recovery ownership — 2026-09-19

Baseline: `5cc831a17aa8971fb5b89c7dd9278a6ec4022beb`; branch `feature/upgrade-18-browser`.

`BrowserService.observe` retained crash-recovery timers after later navigation and after superseding crashes. A stale callback could reload a new page or bypass the three-crash retry cap. Recovery now owns exactly one timer, clears ownership before executing, and cancels on a new main-frame document navigation, manual Reload/Stop, tab close, or another crash. Stop leaves an honest manual-reload error. Same-document and child-frame navigation do not cancel recovery.

Research hypotheses: selected stale crash recovery for demonstrated navigation impact. Deferred the smaller popup/opener listener retention issue. Initial load callbacks already check closing/current URL/ERR_ABORTED. Unowned downloads already cancel and owned downloads use the native Save dialog.

Read-only source lessons; original implementation, no copied source:
- Waku `6d433e875d57091906ec0770d8bb9ffc9aa29b83`, `src/browser.rs` (GPL-3.0): snapshot epochs reject completions after ownership changes. SHA-256 `487c8d0e913d9bff94509f85ed91ba546ece4697c734c5fec52a94b0dae1aafe`.
- OpenCode **v2 study checkout**, `origin/2.0` at `7a6ce05d0939826aa6c8e1c481489a713b2d633f`, `packages/app/src/context/global-sdk.tsx` (MIT): heartbeat cancellation and attempt cleanup. SHA-256 `0073e401d275e1c2a4eee3ad6bf4afcd04e6b8c35e89aa948d7165e204fa2484`.
- Hermes `69ae247cf3dba34a37ab4af8484b96d3559a4fcf`, `tools/browser_tool.py` (MIT): stop supervisor first, clean up the exact session, avoid renewing expired sessions during teardown. SHA-256 `f81314fe84d1ae6878d71f0491a0760645fca61b93e24894858f040263b748cd`.

Regression evidence: two added Electron lifecycle tests fail against baseline. A stale timer after navigation produces one reload instead of zero; superseded/duplicate/exhausted callbacks produce four reloads instead of one. Tests exercise the real BrowserService listeners, real native guest navigation and reload, with controlled crash events/timers; they do not intentionally crash Chromium. Existing `tests/e2e/*.spec.ts` discovery includes these tests without package-script changes.

No shared server, native-client, permission, new capability, or visual layout contract changes. No onboarding or plan-status change is needed. Full validation and PR status follow in this note.

Validation: 140 browser tests pass; application and E2E TypeScript, lint, production build, and diff check pass. Full native browser Electron run: **21 passed, 1 failed**, including both new passing regressions. The failure is `browser.spec.ts:89`: the existing Live floating launcher intercepts the annotation Add to chat button. Reproduced the identical timeout on unchanged baseline `5cc831a`; logs `/tmp/aiden-18-e2e.log` and `/tmp/aiden-18-annotation-baseline.log`. Reported to campaign root and attachment owner; no unrelated UI edit. Hosted CI and central review remain pending.

## Pullfrog: queued native crash notifications

PR #164 review `PRRT_kwDOTctvDc6j9uyL` demonstrated the reverse ordering: navigation starts before the old crash notification is delivered. Verified Electron `v43.1.1` `shell/browser/api/electron_api_web_contents.cc:2045-2065`: `PrimaryMainFrameRenderProcessGone` posts delivery, and details contain only reason/exitCode, with no originating document identity. The pinned Chromium `150.0.7871.114` implementation backs `isCrashed()` with native main-frame process status and resets it when the primary process becomes alive. Source links:
- https://github.com/electron/electron/blob/v43.1.1/shell/browser/api/electron_api_web_contents.cc#L2045-L2065 (SHA-256 `0ee59ca23c4706734a5f46681f9a8cff1b4a71f9d287104bf603227ab1885060`, MIT)
- https://github.com/chromium/chromium/blob/150.0.7871.114/content/browser/web_contents/web_contents_impl.cc#L12486-L12492 (SHA-256 `19371b9936b62a08fd58bc99e47ab1dd9e71cf47ab1d22ae438cbaeabfaa59ae`, Chromium BSD-style license)

Ignore a queued notification when the current native page is no longer crashed or a replacement main-frame load is underway. Do this before touching recovery state; also invalidate recovery at main-frame commit. This uses current native ownership because Electron does not expose the crashed document's identity in the queued event.

Two additional tests forcefully crash actual Electron guest renderers, hold native notification delivery until replacement navigation is pending/committed, and verify no stale retry is scheduled. Both fail published head `fc47fea7` (one unexpected timer each). Fixed lifecycle suite: 8/8 pass. Final extended native cases: 2/2 pass, including a fresh crash after commit with a stalled subresource; genuine current crashes still recover. Browser suite 140/140 and type checks/lint/build pass. Earlier full-suite baseline UI obstruction remains separately recorded above.

## Luna: current crash while the main-frame response is pending

The preceding broad native-loading check was insufficient. A real initial-navigation crash with a held main-frame response reports `isCrashed=true, isLoadingMainFrame=true`; published head `6287ef56` schedules no retry and leaves the browser state falsely uncrashed. The subsequent-navigation control can instead clear the native loading flag, so tests must distinguish these lifecycle states rather than require identical flags.

Capture whether the native renderer is already crashed when a new main-frame navigation starts. Such a navigation supersedes that crash. A crash that happens after navigation began still owns recovery even with a pending main-frame response. Reset suppression when Electron emits `frame-created` for the active primary main frame or commits navigation, so renderer recreation admits fresh crashes. The pinned Electron `RenderFrameCreated` source excludes speculative frames (`electron_api_web_contents.cc:1952-1974`).

The new initial-navigation test also proves `reload()` cannot revive a tab without a committed history entry. Capture the interrupted navigation URL and retry it with `loadURL`; keep ordinary reload for committed documents. Regression coverage requires native recovery to the intended URL and truthful `crashed=true, loading=false` state before recovery. No sleeps or arbitrary delays choose event ownership.

The initial zero-history regression now runs in a standalone native Electron child using the real BrowserService. It asserts empty URL/zero history before death, uses the real retry timer, and observes exactly two requests to the intended URL plus recovered native/service state. This exact test fails `6287ef56` with `{nativeCrashed:true,nativeLoading:true,crashed:false,loading:true}` and passes the corrected source. No about:blank commit weakens the case. The `.spec.ts` wrapper uses existing Playwright discovery; the native entry itself is not separately discovered.

Final verification for this correction: 10/10 lifecycle/native Electron cases pass, 140/140 browser tests pass, and application/E2E TypeScript, lint and build pass. The standalone zero-history regression fails exact prior head `6287ef56337768b5dc717a857b30a602a84f67d3` on the expected service-state mismatch, then passes the corrected source. Its private native owner bypasses IPC admission only; the real BrowserService, Chromium guest, HTTP requests, crash event and retry timer run unchanged.

## Native terminal-navigation controls and abandoned recovery targets

Independent review hypothesized that failed replacement navigation would retain crash suppression indefinitely. Native controls on `629aeb20` do not reproduce that scenario: both HTTP 204 (native `did-stop-loading`, no normal commit) and a connection reset (real `did-fail-load`, native error-page commit) recreate a live renderer. A queued old crash correctly schedules no retry in both cases. No speculative deferred-crash recovery was added.

Pullfrog `PRRT_kwDOTctvDc6j-DAz` / comment `4052610222` identified a distinct reachable bug. After HTTP 204 cancellation or explicit Stop, the committed `/original` page remains current but `pendingNavigationUrl` still points to `/replacement`. A later genuine renderer crash retries the abandoned target. Both exact-`629aeb20` native regressions fail with `{originalRequests:1,replacementRequests:2}` instead of `{originalRequests:2,replacementRequests:1}`. The two queued-notification terminal controls pass. Evidence: `/tmp/aiden18-abandoned-target-baseline.log`.

Move the pending target into tab-owned state so Stop can discard it immediately, and clear it when native loading finishes with a live renderer process. Keep it when a current renderer dies during active navigation, including the zero-history initial-response case. Existing timer ownership and native crash-admission rules remain intact. No new event identity heuristics or recovery budget changes.

Native teardown ordering requires checking the OS process handle as well as `isCrashed()`: a subsequent held-response crash emits `did-stop-loading` while cached `isCrashed` may still be false. Electron 43.1.1 `GetOSProcessID` (`electron_api_web_contents.cc:2617-2620`) reads the primary renderer's native Process and returns zero when invalid. Chromium 150.0.7871.114 `RenderProcessHostImpl::ProcessDied` resets `child_process_launcher_` **before** calling any `RenderProcessExited` observer; `GetProcess()` then returns an invalid process. This provides a synchronous teardown boundary, independent of queued `render-process-gone` delivery or elapsed event-loop turns. The final implementation has no deferred target cleanup or stale cleanup callback. The earlier next-turn candidate was discarded.

Source: https://github.com/chromium/chromium/blob/150.0.7871.114/content/browser/renderer_host/render_process_host_impl.cc#L5573-L5615 and `GetProcess` at lines4041-4055. SHA-256 `281626421255d9bb6dc1f61acaf598779714b46a72901990dff8763b60826c23`; Chromium BSD-style license. Together with the pinned Electron source above, this is the version-specific process-handle ordering relied upon. Aiden uses ordinary out-of-process renderers. Native tests observe PID zero during crash-induced stop and positive PID during healthy canceled/error-page completion without mocking native state. Rerun these native cases when upgrading Electron.

Final native event artifact (`/tmp/aiden18-native-crash-event-order.json`) records `did-stop-loading {crashed:false,loading:false,osProcessId:0}` followed by `render-process-gone {crashed:true,loading:false,osProcessId:0}`. The final full lifecycle/native suite passes **16/16**, zero unexpected/flaky/skipped, in106.1s (`/tmp/aiden18-publication-electron.json`). All four before/after terminal-failure controls pass; both later-current-crash target regressions recover `/original` with exact2/1 request counts. Exact629 before-failure controls also pass2/2 (`/tmp/aiden18-pending-control-baseline.log`), complementing the earlier after-failure controls. Application/E2E TypeScript, lint, full build, 140 browser tests, and diff checks pass. The earlier unrelated full-browser UI obstruction remains outside this lane; hosted CI/independent review are separate gates.

## Closeout: request-owned terminal navigation

The b7 `did-stop-loading` cleanup was insufficient: Chromium's global frame-tree stop may precede completion of the current main-frame navigation. Electron exposes no navigation handle in this event. A deterministic regression controls that documented global stop/loading snapshot while a real native main-frame request remains held, then forcefully crashes the renderer and checks recovery navigates to the held target.

The correction correlates each main-frame request ID with the existing tab navigation generation in the previously unused session `onBeforeRequest` callback. Matching HTTP204/205 response headers definitively retain the committed document; a matching request error clears the target only with a live native renderer. New navigation invalidates request ownership, and global loading-stop cannot discard it. Existing explicit Stop and commit handling remain. The existing session `onBeforeSendHeaders` preview authorization callback is untouched; no duplicate event listener registration is introduced. No new polling, defer window, permission, network destination, shared-client contract, or onboarding capability is added.

Sources: Electron43.1.1 `WebContents::DidFinishNavigation` returns for uncommitted navigation; the official WebRequest contract provides request ID, owning WebContents, resource type and per-request headers/error callbacks. Native HTTP204 probe `/tmp/aiden18-request-events.cjs` confirms no `did-fail-provisional-load` event and `ERR_FAILED` loadURL rejection. The racy global stop boundary is deterministic/source-backed rather than claimed as an independently reproduced natural Chromium race.

Closeout verification: full lifecycle/initial suite **17/17 passed** (2.1min); two additional native request controls (same-URL replacement and HTTP205 followed by later crash) **2/2 passed**. The new controlled global-stop boundary fails exact b7 service with recovered `/start` instead of intended `/held` (`/tmp/aiden18-closeout-b7-red.log`); rebuilt corrected source passes **1/1** (`/tmp/aiden18-closeout-green-final.log`). Browser units **140/140**, application and E2E TypeScript, lint, full build, rebuilt Electron main process and diff check pass. Published CI and independent final review remain separate gates.

## Final local-abort ownership follow-up

Luna identified two locally definitive aborts that still depended on later request callbacks: preview-admission failure inside main-frame navigation start, and the generation-guarded command timeout/cancel catch. Both now discard URL and request ownership before calling native Stop; explicit user Stop discards both too. The existing navigation-generation guard prevents an older command catch from stopping or clearing a later navigation. This is a source-level boundary correction, not a claim of an observed missing native error callback. Two deterministic pre-request tests drive the real expired-preview check and command deadline without a native request callback, then genuinely crash the committed renderer and verify recovery stays on the committed document.

The corrected local-abort cases pass2/2. Exact `be7cfe83` timeout control demonstrably recovers `/original/abandoned` instead of `/original` (`/tmp/aiden18-local-abort-be7-red.log`). The same baseline run's expired-preview case ended with Electron closure and is inconclusive, not claimed as a red proof. The initial fixture attempt failed before either abort because native guest commit had not completed; the corrected fixture awaits the URL before looking up WebContents.

## Final Pullfrog: delayed command start and newer navigation

Review5256522854/comment4053864943 is valid: the old command catch's `startedAtSequence + 1` allowance can claim the first start emitted by a newer navigation while the command's own start is delayed. Same-URL matching alone cannot distinguish these starts. Electron43 exposes no navigation/command identifier in `did-start-navigation` details.

A tab now retains the pending command intent, its queue epoch, and the actual main-frame start sequence. Renderer `will-navigate` and the same-tab popup load invalidate that intent before their navigation, including same-URL targets; user commands already advance the queue epoch, and crashes explicitly retire command intent. Only the same intent, witnessed start sequence, and queue epoch authorize timeout/cancel Stop and target cleanup. Every catch/finally retires its own intent without removing a replacement. Initial tab loads occur before any command intent exists; reload/history commands are serialized through the same queue; recovery follows crash interruption. No unrelated native WebContents caller was found in this service.

Rejected candidate: capturing only starts emitted synchronously inside loadURL is incorrect. Real native direct, redirected, and beforeunload controls all emit start after return, so that candidate silently failed to stop owned loads. The final intent implementation retains their timeout cleanup. Electron's `will-navigate` contract excludes programmatic loadURL, and redirects emit a separate `did-redirect-navigation`; URL changes on a redirect therefore do not invalidate the owned start. Sources inspected: pinned Electron43.1.1 `lib/browser/api/web-contents.ts` `_awaitNextLoad`/`loadURL` and `shell/browser/api/electron_api_web_contents.cc` `DidStartNavigation`/`DidRedirectNavigation`/`LoadURL`, plus installed Electron43 event types. This tracks Aiden's actual navigation entry paths; Electron does not support identifying an arbitrary out-of-band same-URL native load from its start event alone.

New regressions hold the command before its start, then launch real renderer navigation, same-tab popup navigation, or another user command. Different-URL and same-URL replacements must remain loading, receive zero old-command Stops, and recover their held target after an actual renderer crash. Direct, redirected, and beforeunload native command controls must still Stop once and recover the original document after a later real crash. New tests release the real command deadline only after the native HTTP request arrives, avoiding a 500ms scheduling assumption.

Final validation: all four supported-entry regressions fail exact c402 service on unexpected Stop (expected0/received1); baseline built with an esbuild source override without changing the reviewed source files. All seven admission-gated new cases pass. The final complete lifecycle/initial native run passes **28/28 in3.3min**, zero retries/flaky/skipped; includes a denied renderer-navigation control that preserves the owned timeout. Browser140/140, both TypeScript suites, lint, production build and diff checks pass. Evidence `/tmp/aiden18-supported-c402-red.log`, `/tmp/aiden18-intent-gated-green.log`, `/tmp/aiden18-final-native28.log`; durable copies in campaign `pr164-command-ownership-evidence/`. Current-head hosted checks and independent review are separate publication gates.
