# iOS rename response ownership

Isolated feature/ios-rename-response-ownership atop PR23418eddee68833ffdbfaa1e9cbf7f67cd81e7a91dd. PR217 cursor62fa352d is separately integrated; do not duplicate it.

## Implementation

A successful delayed PATCH owns a title field, not a whole older transcript/revision. Reserve its request origin before HTTP and receipt cutoff synchronously before the cache actor hop. Keep a separately persisted RenameTitle record, with a presentation-only localTitleOverride excluded from AidenChat CodingKeys; canonical cache encoding and admitted canonical objects retain coherent server fields. Workspace rows, detail navigation title, summary projection and Live Activities use displayTitle. Canonical detail initial/reconcile/title GETs now reserve before HTTP and use saveFetchedChat.

The title overlay survives pre-receipt reads and retires only after a later-requested admitted coherent GET/full workspace LIST. Receipt and fetched high-water marks fence actor mailbox reordering and overlapping PATCH completion orders. Retirement writes an atomic explicit retired-state record rather than relying on unlink; a failed canonical write preserves the fresh memory winner and separately records its title for restart. Remove/purge clear or fence overlay state. Post-purge disk fallback remains blocked.

Workspace rename refreshes read-only after a successful PATCH, never replays it, and requires a coherent GET before another revisioned rename/remove while an overlay is pending. Failed refresh retains the successful title. The companion reads current canonical state after the metadata suspension and synchronously upserts list/summary without inventing transcript authority. Post-receipt authoritative metadata omissions remain absent; a newer list row wins presentation over older detailed-cache state. Only actual HTTP list admission may retire a title overlay, not locally queued metadata snapshots. Unrelated chat deletion does not suppress a retained rename.

## Regression/validation evidence

Ten new XCTest methods cover real held PATCH against newer settlement, removal, purge and unrelated deletion; failed refresh with separately reopened title/canonical records; no next PATCH/DELETE until coherent refresh and valid If-Match; successful list/summary reopen; failed unlink and canonical IO; both PATCH/GET orders; fresh GET during suspended receipt persistence and before actor entry; overlapping newer/older receipts; held metadata companion versus older-reserved detail GET; post-receipt empty list/summary; production callback/model/reopened-cache adoption of a newer full-list row; and a held older summary released after the winning list companion. The previous empty-list rename test now distinguishes a valid unrelated row update from a newer authoritative omission. These are compiled regressions, not executed iOS tests or an executed baseline failure.

Generic app/XCTest build-for-testing: /tmp/aiden-ios-rename-response-build.log. Android JVM/lint/instrumentation compilation: /tmp/aiden-ios-rename-response-android.log (177 tests). iOS policy: /tmp/aiden-ios-rename-response-policy.log (31 Node, 20 Ruby/42 assertions). Initial policy source-regex failure was caused by changing the unrelated Bot header title fallback; that binding was restored and checks pass. Final review clearance/publication will be recorded below.

Physical acceptance remains unexecuted; shared iPhone passcodeRequired true, no slot or launch. Remaining assigned work is iOS Home summary response admission and PR CI/review follow-through; this patch does not claim that Home race fixed.

Final summary reconciliation carries the selected list/detail winner token, preventing an already-held older summary from replacing it. Passed presentation objects with a local title override also require coherent refresh before rename/remove, even if another owner has already retired shared overlay state. Final generic XCTest compilation and policy checks pass; Android177 tests/lint/instrumentation remain unchanged and green. Both independent GPT-5.6 Sol medium final full-roster reviews are clear.

## Integration correction: list title across detail hydration

Combined review found that a later authoritative LIST could admit title C but throw before retiring rename B; even successful retirement exposed an older canonical detail title A/B to generic detail hydration. A later authoritative list now supersedes the separate title record in a defer after admission, whether list persistence succeeds or fails. It retains C separately until a coherent detail GET catches up; canonical transcript/revision remains untouched and later revisioned mutations retain the read prerequisite. Actual HTTP title provenance is kept separately from locally replaced workspace snapshots, so a mailbox-late PATCH cannot select a locally replayed old title instead of the newer HTTP title. Removal/purge clear this provenance.

Two additional XCTest methods cover sixteen combinations including empty/nonempty authoritative titles of HTTP/local list, pre/post-receipt reservation and successful/failed list IO, checking workspace and generic detail presentation, unchanged canonical title/revision, restart, and later coherent detail retirement; plus HTTP C followed by local snapshot B before actor-late PATCH admission. Generic compilation passes /tmp/aiden-ios-rename-list-failure-build.log, unchanged177 Android JVM/lint/instrumentation checks pass /tmp/aiden-ios-rename-list-failure-android.log, policy31Node20Ruby42 passes /tmp/aiden-ios-rename-list-failure-policy.log. These regressions are compiled only; physical tests remain unexecuted. Both independent GPT-5.6 Sol medium final correction reviews are clear. Corrected-head integration acceptance remains required.

Review also caught the valid empty-title restart case: new title records encode an explicit retired flag; missing legacy flags retain the prior empty-title tombstone interpretation. Empty authoritative titles now survive restart. Final generic compile and policy checks pass in /tmp/aiden-ios-rename-empty-title-build.log and /tmp/aiden-ios-rename-empty-title-policy.log.
